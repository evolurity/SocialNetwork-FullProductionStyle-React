{
  "compilerOptions": {
    "noImplicitAny": true, //нельзя не указывать тип
    "module": "ESNext",
    "target": "es5", //в какую специф. комплир. для поддержки в большинстве браузеров
    "jsx": "react-jsx",
    "allowJs": true,
    "moduleResolution": "node",
    "baseUrl": ".",
    "paths": {
      "*": ["./src/*"]
    },
    //  esModuleInterop позволяет работать с пакетами, которые используют
    // common js как с обычными пакетами с помощью (import require() module.export) = common js
    "esModuleInterop": true,
    // для библиотек, не поддерж.  default import, чтобы не писать import * as lib from "lib"
    "allowSyntheticDefaultImports": true
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  },
  "exclude": [
    "node_modules"
  ]
}